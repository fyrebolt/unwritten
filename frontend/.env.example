# Unwritten API (denial parse + Claude intake transcription). Run: pnpm dev:backend from repo root.
NEXT_PUBLIC_API_URL=http://localhost:8787

# Cloudinary (https://console.cloudinary.com/)
# NEXT_PUBLIC_CLOUDINARY_UPLOAD_PRESET = preset NAME (not the PID line in the UI).
# Optional: only if your preset allows client-side folder; must match Asset folder if restricted.
# NEXT_PUBLIC_CLOUDINARY_UPLOAD_FOLDER=unwritten/denials
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=dtuelki2p
NEXT_PUBLIC_CLOUDINARY_UPLOAD_PRESET=unwritten_unsigned
